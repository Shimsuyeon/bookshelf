enum Paths {
  main = "/",
  bookshelf = "/bookshelf",
  books = "/books",
  bookdetail = "/bookdetail",
  firstStory = "/firstStory",
  post = "/post",
  booklist = "/booklist",
  bookinfo = "/bookinfo",
}

export default Paths;
